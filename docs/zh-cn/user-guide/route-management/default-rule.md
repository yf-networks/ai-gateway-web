# 默认转发规则

## 默认转发规则概述

AI_product产品线必须设置默认规则，以指定未命中其他规则时的默认目标集群。

新创建的配置中，默认规则的目标集群为空，需要编辑以进行设置。

## 配置默认转发规则

- 在左侧菜单，进入"路由管理"->"默认转发规则"页，为默认规则设定一个目标集群

![默认转发规则页面](../../../images/AI-gateway-rule-default.png)

- 默认规则只需要设置目的集群
